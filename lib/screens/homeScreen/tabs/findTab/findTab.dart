import 'package:flutter/cupertino.dart';

class FindTab extends StatelessWidget {
  const FindTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('FindTab'),);
  }
}
